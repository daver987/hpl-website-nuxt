export default defineNuxtRouteMiddleware((to, from) => {
	// const userQuoteData = useState('quote-data')
	// if ((userQuoteData.value = null)) {
	// 	return abortNavigation()
	// }
	// return navigateTo('/quoted')
})
